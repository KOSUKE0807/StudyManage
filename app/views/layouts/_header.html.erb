<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
  <div class="container-lg">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand" href="/home" data-bs-toggle="tooltip" data-placement="bottom" title="ホーム画面へ">StudyManage</a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/home" data-bs-toggle="tooltip" data-placement="bottom" title="ホーム画面へ"><i class="fas fa-home"></i> ホーム</a>
        </li>
        <% if @current_student %>
          <li class="nav-item active">
            <a class="nav-link" href="/study_records" data-bs-toggle="tooltip" data-placement="bottom" title="学習を記録する">学習記録</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="#" data-bs-toggle="tooltip" data-placement="bottom" title="単語テストを受ける">単語学習</a>
          </li>
          <li class="nav-item active">
            <a class="nav-link" href="/questions" data-bs-toggle="tooltip" data-placement="bottom" title="先生に質問する">質問</a>
          </li>
        <% else %>
        <% end %>
      </ul>
    </div>
    <a style="text-align: center;" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
      <% if @current_user.image.present? %>
        <%=image_tag @current_user.image.url, class: "user-profile-img-self" %>
      <% else %>    
        <i class="navbar-brand fas fa-user-circle" style="font-size: 32px;"></i>
      <% end %>
      <p style="margin-bottom: 0px;font-size: 10px;color: white;"><%=@current_user.user_name %></p>
    </a>
  </div>
</nav>
<div class="collapse" id="collapseExample">
  <div class="container-sm" style="text-align: right;">
    <div>
      <p style="margin-bottom: 0px;font-size: 17px;"><%=@current_user.user_name %></p>
      <% if @current_student %>
        <p style="margin-bottom: 0px;font-size: 15px;">担当の先生：<%=@current_user.teacher.user_name %></p>
      <% end %>
    </div>
    <div class="btn-group" role="group" aria-label="Basic example">
      <% if @current_student %>
        <%= link_to "プロフィール編集", edit_student_path(@current_student.id), class: "btn btn-outline-primary btn-sm" %>
      <% else %>
        <%= link_to "プロフィール編集", edit_student_path(@current_teacher.id), class: "btn btn-outline-primary btn-sm" %>
      <% end %>
      <button type="button" class="btn btn-outline-primary btn-sm" style="color: red;">
        <%= link_to "ログアウト", logout_path, method: :delete %>
      </button>
    </div>
  </div>  
</div> 